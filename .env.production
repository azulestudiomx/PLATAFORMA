# .env.production
# Este archivo es usado automáticamente por Vite al hacer "npm run build"
# VITE_API_URL vacío = el frontend usa URLs relativas (/api/...)
# Nginx en el VPS hace el proxy de /api → backend
VITE_API_URL=
